node_modules
.env
.env.*
*.log

# Transient working data (STEM_DIR) — separated stems written per job, swept on a TTL
.data/

# Secrets — never commit service-account keys or credential files
credentials/
*.serviceaccount.json
*-credentials.json

# GCP deploy — filled secrets, never commit
deploy/gcp/plugin.env
deploy/gcp/*.out
